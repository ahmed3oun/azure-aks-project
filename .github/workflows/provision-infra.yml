name: Provision Infrastructure

on:
  workflow_dispatch:
    inputs:
      workspace:
        description: "Select our Terraform workspace to use"
        required: true
        default: "dev"
        type: choice
        options:
          - dev

permissions:
  id-token: write
  contents: read
jobs:
  terraform-apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          enable-AzPSSession: true

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.0
      - name: Init
        working-directory: ./
        run:  terraform init -backend-config="resource_group_name=${{ secrets.TF_BACKEND_RG }}" -backend-config="storage_account_name=${{ secrets.TF_BACKEND_STORAGE_ACCOUNT }}" -backend-config="container_name=${{ secrets.TF_BACKEND_CONTAINER }}" -backend-config="key=${{ secrets.TF_BACKEND_KEY }}"
      - name: Terraform workspace select
        working-directory: ./
        run: terraform workspace select ${{ github.event.inputs.workspace }} || terraform workspace new ${{ github.event.inputs.workspace }}
      - name: Terraform Plan
        working-directory: ./
        run: terraform plan
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Terraform Apply
        working-directory: ./
        run: terraform apply -auto-approve
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}


# name: Provision Infrastructure

# on:
#   workflow_dispatch: # Allow manual triggering of the workflow
#     inputs:
#       environment:
#         description: "Select the environment to provision (dev, staging, prod)"
#         required: true
#         default: "dev"
#         type: choice
#         options:
#           - dev
# permissions:
#   id-token: write # Allow the workflow to request an OIDC token for authentication
#   content: read # Allow the workflow to read repository content (e.g., for checking out code)
# jobs:
#   terraform-apply:
#     name: Terraform Apply
#     runs-on: ubuntu-latest
#     steps:
#       # Checkout the repository to access Terraform configuration files
#       - name: Checkout Repository
#         uses: actions/checkout@v3
#       # Login to Azure using OIDC for secure authentication without storing secrets in the repository
#       - name: Azure Login using OIDC
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.AZURE_CLIENT_ID }} # Azure AD Application (Service Principal) Client ID
#           tenant-id: ${{ secrets.AZURE_TENANT_ID }} # Azure AD Tenant ID
#           subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }} # Azure Subscription ID
#           enable-AzPSSession: true # Enable Azure PowerShell session for running Azure CLI commands if needed
#       # Set up Terraform CLI for running Terraform commands
#       - name: Set up Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: 1.14.0 # Specify the version of Terraform to use
#       # Initialize Terraform to set up the working directory and download necessary providers
#       - name: Terraform Init
#         working-directory: ./ # Change to the directory containing Terraform configuration files
#         run: terraform init -backend-config="resource_group_name=${{ secrets.TF_BACKEND_RG }}" -backend-config="storage_account_name=${{ secrets.TF_BACKEND_STORAGE_ACCOUNT }}" -backend-config="container_name=${{ secrets.TF_BACKEND_CONTAINER }}" -backend-config="key=${{ secrets.TF_BACKEND_KEY }}" # Initialize Terraform with backend configuration for remote state storage
#       # Select or create the Terraform workspace based on the environment input provided when triggering the workflow
#       - name: Terraform workspace select
#         working-directory: ./ # Change to the directory containing Terraform configuration files
#         run: terraform workspace select ${{ github.event.inputs.environment }} || terraform workspace new ${{ github.event.inputs.environment }}
#       # Run Terraform plan to see the changes that will be applied to the infrastructure without making any actual changes
#       - name: Terraform Plan
#         working-directory: ./ # Change to the directory containing Terraform configuration files
#         run: terraform plan
#         env:
#           ARM_USE_OIDC: true # Enable the use of OIDC for authentication with Azure
#           ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }} # Azure Subscription ID for authentication
#           ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }} # Azure AD Tenant ID for authentication
#           ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }} # Azure AD Application (Service Principal) Client ID for authentication
#       # Run Terraform apply to apply the planned changes to the infrastructure, creating or updating resources as defined in the Terraform configuration files
#       - name: Terraform Apply
#         working-directory: ./ # Change to the directory containing Terraform configuration files
#         run: terraform apply -auto-approve # Apply the changes without prompting for confirmation
#         env:
#           ARM_USE_OIDC: true # Enable the use of OIDC for authentication with Azure
#           ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }} # Azure Subscription ID for authentication
#           ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }} # Azure AD Tenant ID for authentication
#           ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }} # Azure AD Application (Service Principal) Client ID for authentication

